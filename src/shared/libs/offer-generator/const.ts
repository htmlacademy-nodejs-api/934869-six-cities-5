export enum OfferRating {
  MIN = 0,
  MAX = 5
}

export enum Rooms {
  MIN = 1,
  MAX = 8
}

export enum Guests {
  MIN = 1,
  MAX = 10
}

export enum Price {
  MIN = 100,
  MAX = 100000
}

export enum Comments {
  MIN = 0,
  MAX = 25
}

export enum WeekDay {
  MIN = 1,
  MAX = 7
}
